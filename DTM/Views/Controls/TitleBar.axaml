<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="DTM.Views.Controls.TitleBar"
             Height="36"
             Background="{DynamicResource KrosteTitleBarBrush}">
    <!-- Wiederverwendbare Standard-Titelleiste fuer DTM-Dialoge:
         Title-Text links, optional Min/Max, immer Close rechts. Pointer-
         Handling laeuft ueber Code-Behind (nicht ueber Avalonia-12-
         ElementRole), weil DTM den etablierten IsHitTestVisible-Weg
         nutzt und der cross-plattform stabil ist. MainWindow behaelt
         seine eigene Titelleiste (Sonder-Header-Buttons wie Settings/About). -->
    <Border BorderBrush="{DynamicResource KrosteBorderBrush}" BorderThickness="0,0,0,1"
            PointerPressed="OnBarPointerPressed"
            DoubleTapped="OnBarDoubleTapped">
        <Grid ColumnDefinitions="*,Auto">
            <!-- Glyph + Text als eine Einheit: die meisten Dialoge fuehren ein
                 kleines Piktogramm vor dem Titel (Warnung, Zahnrad, Restore-
                 Pfeil). Ohne gesetztes Glyph bleibt der TextBlock unsichtbar
                 und das Layout entspricht exakt der reinen Text-Variante. -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8"
                        VerticalAlignment="Center" Margin="14,0"
                        IsHitTestVisible="False">
                <TextBlock x:Name="GlyphText" FontSize="13"
                           VerticalAlignment="Center" IsVisible="False" />
                <TextBlock x:Name="TitleText"
                           FontSize="12" FontWeight="SemiBold"
                           Foreground="{DynamicResource KrosteMutedTextBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch">
                <!-- Platz fuer fensterspezifische Chrome-Buttons links von
                     Min/Max/Close (MainWindow haengt hier sein "Ueber"-i ein). -->
                <ContentControl x:Name="ExtraSlot" />
                <Button x:Name="MinButton" Classes="chrome"
                        Click="OnMinClick" Content="&#x2013;"
                        IsVisible="False" />
                <Button x:Name="MaxButton" Classes="chrome"
                        Click="OnMaxClick" Content="&#x2750;"
                        IsVisible="False" />
                <Button x:Name="CloseButton" Classes="chrome close"
                        Click="OnCloseClick" Content="&#x2715;" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
