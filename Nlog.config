<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   autoReload="true"
 internalLogLevel="Warn"
internalLogFile="${basedir}/logs/internallog.txt">

  <targets>
    <target name="coloredConsole" xsi:type="ColoredConsole" useDefaultRowHighlightingRules="false"
    layout="${longdate}|${pad:padding=5:inner=${level:uppercase=true}}|${masked:inner=${message}}" >
      <highlight-row condition="level == LogLevel.Trace" foregroundColor="Magenta" />
      <highlight-row condition="level == LogLevel.Debug" foregroundColor="DarkGray" />
      <highlight-row condition="level == LogLevel.Info" foregroundColor="Gray" />
      <highlight-row condition="level == LogLevel.Warn" foregroundColor="Yellow" />
      <highlight-row condition="level == LogLevel.Error" foregroundColor="Red" />
      <highlight-row condition="level == LogLevel.Fatal" foregroundColor="Red" backgroundColor="White" />
    </target>

    <!-- ${masked:inner=...} greift auf Message UND Exception-Rendering zu:
         der Wrapper-Renderer ersetzt Passwoerter/Tokens/Credentials durch ***
         (Registrierung in Diagnostics/MaskingLayoutRenderer.cs). -->
    <target name="infoFile" xsi:type="File"
        layout="${longdate} ${pad:padding=5:inner=${level:uppercase=true}} ${logger} ${masked:inner=${message}}${onexception:inner=${newline}${masked:inner=${exception:format=tostring,Data:maxInnerExceptionLevel=10}}}"
        fileName="${basedir}/logs/info.log" keepFileOpen="false" encoding="utf-8" />
    <target name="errorFile" xsi:type="File"
        layout="${longdate} ${pad:padding=5:inner=${level:uppercase=true}} ${logger} ${masked:inner=${message}}${onexception:inner=${newline}${masked:inner=${exception:format=tostring,Data:maxInnerExceptionLevel=10}}}"
        fileName="${basedir}/logs/error.log" keepFileOpen="false" encoding="utf-8" />
  </targets>

  <rules>
    <logger name="*" minlevel="Info" writeTo="coloredConsole" />
    <logger name="*" minlevel="Info" maxlevel="Warn" writeTo="infoFile" />
    <logger name="*" minlevel="Error" maxlevel="Fatal" writeTo="errorFile" />
  </rules>
</nlog>